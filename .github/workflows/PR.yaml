name: 'Pull Request'
description: 'Validate all yaml files using actionlint'

on:
  pull_request:
    paths:
      - '.github/workflows/**.yaml'
      - '.github/workflows/**.yml'

jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Run actionlint
        uses: raven-actions/actionlint@v1